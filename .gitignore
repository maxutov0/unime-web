# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env and local
.env
.env.local
.DS_Store

# SQLite (sql.js) persisted DB
server/data.sqlite
server/data.sqlite-journal
server/data.sqlite-wal

# Build artifacts (if added later)
dist/
build/
*.log
